// import { render } from "@testing-library/react";
// import App from "./App";

describe("test", () => {
  // it("redirects to login page", () => {
  //   render(<App />);
  // });

  it("test", () => {
    expect(2).toEqual(2);
  });
});
